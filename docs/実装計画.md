<!--
【テンプレート】AIチャット機能 実装計画書
「AIチャット機能 要件定義書」に基づき、Next.js + Vercel AI SDKを使った実装手順を示す計画書です。
プレースホルダー（{{ }}）をプロジェクト固有の情報に置き換えて使用してください。
-->

# {{FEATURE_NAME / 例：AIチャット機能}} 実装計画書

## 1. 実装フェーズ

### Phase 1: 環境構築とパッケージインストール
- Vercel AI SDK 関連パッケージのインストール
- AI SDK Elements UI のセットアップ
- Streamdown のインストール
- {{LLM_PROVIDER_SETUP / 例：OpenAI API キーの設定}}

### Phase 2: API 実装
- チャット API エンドポイントの作成
- {{LLM_PROVIDER}}統合とプロンプト設計
- ストリーミングレスポンスの実装
- {{API_GATEWAY_SETUP / 例：Vercel AI Gateway 設定（オプション）}}

### Phase 3: UI 実装
- AI SDK Elements を使用したチャットコンポーネント作成
- フローティングボタンの実装
- レスポンシブデザインの適用
- Streamdown によるマークダウンレンダリング

### Phase 4: 統合とテスト
- {{PAGE_NAME / 例：詳細ページ}}への組み込み
- {{TARGET_ENTITY / 例：エンティティ}}コンテキストの受け渡し
- エラーハンドリング
- 動作確認とバグ修正

## 2. 詳細実装計画

### 2.1 パッケージインストール

```bash
# web（フロントエンド）ディレクトリで実行
# Core AI SDK
pnpm add ai @ai-sdk/react

# AI Elements (コンポーネントライブラリ)
npx ai-elements@latest

# Streamdown
pnpm add streamdown
```

### 2.2 環境変数設定

`.env`等に追加:

```env
# {{LLM_API_KEY_DESC / 例：Vercel AI Gateway API Key または OpenAI API Key}}
{{ENV_VAR_NAME / 例：AI_GATEWAY_API_KEY}}=your-api-key
```

### 2.3 ディレクトリ構造

```text
{{FRONTEND_ROOT_DIR / 例：web/src/}}
├── features/
│   └── chat/                 # チャットモジュール
│       ├── components/
│       │   ├── chat-button.tsx
│       │   ├── chat-window.tsx
│       │   ├── chat-messages.tsx
│       │   └── index.ts
│       ├── hooks/
│       │   └── use-chat.ts
│       └── types/
│           └── index.ts
└── app/                      # Next.js App Router
    └── api/
        └── chat/
            └── route.ts
```

### 2.4 実装詳細

#### API 実装 (`app/api/chat/route.ts`)

```typescript
import { streamText } from 'ai';
// 必要に応じてプロバイダをインポート (例: import { openai } from '@ai-sdk/openai')

export async function POST(req: Request) {
  const { messages, {{entityContext}} } = await req.json();

  const systemPrompt = `
    あなたは{{ASSISTANT_ROLE}}です。
    
    情報：
    - 名称: ${ {{entityContext}}.name }
    - 説明: ${ {{entityContext}}.description || "" }
    - 詳細: ${ {{entityContext}}.content || "" }
    - {{DOMAIN_SPECIFIC_DATA_LABEL}}: ${ {{entityContext}}.{{domainSpecificData}}?.value || "" }
    
    ルール：
    1. この{{TARGET_ENTITY}}に関する質問にのみ回答する
    2. 分かりやすく簡潔に説明する
    3. 専門用語は必要に応じて解説する
    4. 無関係の質問には「申し訳ございません。この{{TARGET_ENTITY}}に関する質問にお答えします」と返答する
  `;

  const result = streamText({
    model: "{{AI_MODEL_NAME / 例：openai/gpt-4o-mini}}", // プロバイダー/モデル形式で指定
    system: systemPrompt,
    messages,
    apiKey: process.env.{{ENV_VAR_NAME}}, // 必要な場合
  });

  return result.toDataStreamResponse();
}
```

#### チャットボタン (`features/chat/components/chat-button.tsx`)

```typescript
"use client";

import { MessageCircle } from "lucide-react";
import { useState } from "react";
import { ChatWindow } from "./chat-window";
// import types

interface ChatButtonProps {
  {{entityContext}}: {
    id: string;
    name: string;
    description?: string;
    content?: string;
    {{domainSpecificData}}?: {
      // type
    };
  };
}

export function ChatButton({ {{entityContext}} }: ChatButtonProps) {
  const [isOpen, setIsOpen] = useState(false);

  return (
    <>
      <button
        onClick={() => setIsOpen(true)}
        className="fixed bottom-4 right-4 z-50 rounded-full bg-primary p-4 text-white shadow-lg hover:bg-primary/90 transition-colors md:bottom-8 md:right-8"
        aria-label="{{CHAT_TITLE / 例：この内容について質問する}}"
      >
        <MessageCircle size={24} />
      </button>

      {isOpen && (
        <ChatWindow 
          {{entityContext}}={ {{entityContext}} }
          onClose={() => setIsOpen(false)} 
        />
      )}
    </>
  );
}
```
