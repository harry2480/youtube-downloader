<!--
【テンプレート】インフラストラクチャ規約
プロジェクトのインフラ基盤、モノレポ構成の管理、デプロイメント環境に関する規約です。
プレースホルダー（{{ }}）をプロジェクト固有の情報に置き換えて使用してください。
-->

# {{PROJECT_NAME / 例：〇〇プロジェクト}} インフラ規約

{{PACKAGE_MANAGER / 例：pnpm workspace}} + {{HOSTING_PLATFORM / 例：Vercel}} + {{DB_SERVICE / 例：Supabase}} によるインフラ環境の構成と規約。

---

## {{REPO_STRUCTURE / 例：Monorepo}} 構成

{{PACKAGE_MANAGER}} で管理。`{{APPS_DIR / 例：apps/}}` 配下に各アプリケーションを配置する。

```text
{{PROJECT_ROOT}}
├── {{APPS_DIR}}
│   └── {{WEB_APP_DIR / 例：webapp/}}    # {{FRAMEWORK}} アプリ
├── {{WORKSPACE_CONFIG / 例：pnpm-workspace.yaml}}
├── {{LINTER_CONFIG / 例：biome.json}}        # 共有 lint/format 設定
└── package.json         # workspace scripts
```

---

## デプロイメント

| 環境 | トリガー | プラットフォーム | 備考 |
|---|---|---|---|
| {{ENV_PREVIEW / 例：Preview}} | PR 作成 / 更新 | {{HOSTING_PLATFORM}}（自動プレビュー、PR ごとに一意の URL） | |
| {{ENV_PROD / 例：Production}} | main ブランチへの push | {{HOSTING_PLATFORM}}（自動本番デプロイ） | |

### {{HOSTING_PLATFORM}} 設定

- Framework Preset: {{FRAMEWORK_NAME / 例：Next.js}}
- Node.js Version: {{NODE_VERSION / 例：20.x}}
- Build Command: `{{BUILD_CMD / 例：pnpm build}}`
- Install Command: `{{INSTALL_CMD / 例：pnpm install}}`

---

## {{DB_SERVICE}} 環境

- **DBのマイグレーション/シード**: `{{DB_MIGRATE_CMD / 例：supabase db push}}` 等によるスクリプト管理
- **環境変数**: Preview環境とProd環境で別々の {{DB_SERVICE}} プロジェクト（API Key/URL）を紐付ける
- **ローカル開発**: ローカルの `{{LOCAL_CLI_CMD / 例：supabase start}}` でエミュレーター環境を推奨
